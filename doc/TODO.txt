Manual:
	credit til gloom
	+safeimport -> unsafeimport
	+beskriv summary
	+verbose -> ?
	+truncation?
	credit til flere personer?
	-nævn at verbose functions ikke virker i vs8
	skriv faq
	+ws2003
	truncate default 64

Licens:
	+inkluder Distorm-licens
	+formuler Crinkler-licens

Code:
	+report: layout ved lange idents
	*report: lav relocs til links
		*escape idents
	+report: colapse/expand
	+report: lav sidste justeringer af layout og skriv forklaringstekst
	+report: adskillelse af dwords i hexdump
	report: highlight af trunkerede floats og absolutte jump offsets
	+report: hand cursor on labels
	+report: include compression options in report
	+report: fix size table to include highest category
	report: place labels into right section
	report: size of selected range

	-revert to old non-range import code?

	better report for recompression
	replacedll for recompression

	+max ordertries
	button in progressgui to skip phase
	Sensible total time printing
	Remove delay on exit?

	+truncation?
	+verbose -> ?
	combine identical read-only symbols
        -verbose:functions

	Remove 64k limit on uncompressed size (unfix BSS address)

	Make forwarded RVA a warning instead of error? (For Wine crosscompilation)

	automatic dynamic initializers

	+place import code freely (test using KK's code)

	+more than 255 transformed calls
	do not apply call transform when there are no calls
	Use header space for extra push for call trans address

	+override alignments
	+alignment not at the start of section
	+align absolute address
	+/OVERRIDEALIGNMENTS option

	+hint for security_cookie
	+hint for alloca, __chkstk
	+hint for ImageBase

test:
	+Test weak externals support
	+recursive call bug
	+Windows Server 2003
